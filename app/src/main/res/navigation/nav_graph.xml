<?xml version="1.0" encoding="utf-8"?>

<!-- This file controls fragment navigation -->
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/eventList">

    <!-- This is the fragment for the event list -->
    <fragment
        android:id="@+id/eventList"
        android:name="com.example.personaleventplanner.EventList"
        android:label="Event List" />

    <!-- This is the fragment for adding events -->
    <fragment
        android:id="@+id/addEvent"
        android:name="com.example.personaleventplanner.AddEvent"
        android:label="Add Event" />

</navigation>
<?xml version="1.0" encoding="utf-8"?>

<!-- This layout is used inside the edit dialog -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- This input edits the title -->
    <EditText
        android:id="@+id/title_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter title"
        android:layout_marginBottom="12dp" />

    <!-- This spinner edits the category -->
    <Spinner
        android:id="@+id/dropdown_menu1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <!-- This input edits the location -->
    <EditText
        android:id="@+id/location_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter location"
        android:layout_marginBottom="12dp" />

    <!-- This input edits the date and time -->
    <EditText
        android:id="@+id/date_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter date and time (yyyy-MM-dd HH:mm)" />

</LinearLayout>